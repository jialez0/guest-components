[Unit]
Description=Trustiflux API Server (REST bridge for attestation-agent and confidential-data-hub)
After=network-online.target attestation-agent.service confidential-data-hub-daemon.service
Wants=network-online.target attestation-agent.service confidential-data-hub-daemon.service

[Service]
Type=simple
ExecStart=/usr/bin/trustiflux-api-server --config /etc/trustiflux/trustiflux-api-server.toml
Restart=on-failure
RestartSec=1s

[Install]
WantedBy=multi-user.target
